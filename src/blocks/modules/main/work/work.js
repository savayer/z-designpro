import Vue from '../../../../js/import/vue.esm.browser'

new Vue({
    el: '#app',
    data: {
        projectName: 'Test project',
        projectDescription: 'Full Branding Model',
        showGoToSite: true,
        viewProject: false
    }
})

/* if (sessionStorage.getItem('was_here') !== 'true') {
    sessionStorage.setItem('was_here', true)
} */